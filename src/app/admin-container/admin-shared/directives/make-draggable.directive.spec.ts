import { MakeDraggableDirective } from './make-draggable.directive';

describe('MakeDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
