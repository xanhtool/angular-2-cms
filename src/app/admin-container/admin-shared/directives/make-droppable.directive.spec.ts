import { MakeDroppableDirective } from './make-droppable.directive';

describe('MakeDroppableDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeDroppableDirective();
    expect(directive).toBeTruthy();
  });
});
